# Dotfiles

My declarative macOS configuration using:

- nix-darwin
- Home Manager
- nix-homebrew

## Bootstrap

```bash
git clone https://github.com/Aneeshie/dotfiles ~/.dotfiles
cd ~/.dotfiles
./rebuild.sh
```

Dont forget to change the git credentials and home directory names
